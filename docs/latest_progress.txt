
• 目前进展可以分成三层看。

  一、v1 最小闭环已经有了
  这几条已经实现并能跑通：

  - registry
      - scripts/collect_registry.py
      - scripts/stage_registry.py
  - links
      - scripts/collect_links.py
      - scripts/stage_links.py
  - stage1
      - scripts/build_stage1_suspects.py
  - 校验器
      - scripts/validate_outputs.py

  当前这部分验证是通过的：

  - pytest -q：15 passed
  - python3 scripts/validate_outputs.py --stage all
      - registry: ok
      - links: ok
      - prefixes: ok
      - stage1: ok

  二、registry 已经不是离线占位了
  这一块已经接了真实源：

  - delegated
  - RDAP

  现在 collect_registry.py --online 会把真实 raw 落到：

  - data/raw/registry/delegated/
  - data/raw/registry/rdap/
  - data/raw/registry/manifest/

  stage_registry.py 只读 raw，不联网。
  也已经支持行政冲突相关字段，比如：

  - allocated_country
  - registered_country
  - admin_conflict_flag
  - multi_country_registry_flag

  三、links 也已经接了真实源
  这块现在是双源：

  - RIPEstat
      - 提供 observed neighbours 和前后窗口变化
  - CAIDA AS Rank
      - 提供 provider / customer / peer 关系

  raw 会落到：

  - data/raw/links/ripestat/
  - data/raw/links/asrank/
  - data/raw/links/manifest/

  stage_links.py 当前能产出：

  - observed_neighbor_count
  - provider_count
  - customer_count
  - peer_count
  - unknown_count
  - new_neighbor_count
  - lost_neighbor_count
  - neighbor_churn_rate
  - topology_anomaly_flag
  - border_as_flag

  但还没做更强的历史关系切换分析，provider_switch_count 现在还是 0。

  四、prefixes 这条线刚落地了第一版
  这是你最近推动的重点：不在线拉前缀，而是直接用本机 rrc25 的 bview 做月度缓存。

  已经新增：

  - scripts/collect_prefixes.py
  - scripts/stage_prefixes.py
  - docs/schema_prefixes.md

  当前路线是：

  - 本机 bview 根目录：/home/bgpdata/data/ripe/rrc25
  - ASN 子集来源：/home/experiment/info/as_entity.csv
  - 当前试点国家：IR
  - 按月扫描一次 bview
  - 为每个 (asn, month) 落 raw evidence
  - staging 输出：
      - data/staging/prefixes/asn_prefix_inventory_monthly.csv

  这条线已经做过烟雾验证：

  - python3 scripts/collect_prefixes.py --month 2026-03 --country IR --pilot-limit 10 --max-lines 1000000
      - 成功写出 10 个 raw prefix records
  - python3 scripts/stage_prefixes.py --month 2026-03 --country IR
      - 成功写出 10 条 staging rows
  - python3 scripts/validate_outputs.py --stage prefixes
      - prefixes: ok

  也就是说，“本机 bview -> raw -> staging -> validator” 这条链已经通了。
  这次路线调整已经写入：

  - README.md
  - docs/execution_plan.md
  - docs/schema_prefixes.md
  - configs/pipeline.yaml

  文档里已经明确了：

  - rrc25 本机 bview 是前缀集合源
  - as_entity.csv 是国家子集来源
  - 先做伊朗 ASN 试点
  - 按月预计算，不按查询现算

  六、现在还没完成的部分
  目前还没做完的是这些：

  - prefix_geo
      - 现在只有 prefix inventory，还没有 dominant_prefix_country
      - 也没有 foreign_prefix_coverage_ratio
      - geo_conflict_flag 还没接进 stage1
  - infra / geo / path 的正式接入还没开始
  - 最终评分、案例卡、人工复核材料还没展开
  - stage1 现在还是：
      - registry + links
      - geo_conflict_flag 还是占位

  七、当前最合理的下一步
  按现在这条路，最顺的是：

  1. 先把 collect_prefixes.py 跑完整月伊朗缓存
     不带 --max-lines
  2. 再基于这个缓存做 prefix_geo
  3. 然后把 geo_conflict_flag 接进 stage1

  一句话总结：
  当前仓库已经完成了 registry + links + stage1 的最小闭环，且新增打通了“本机 bview 按月预计算伊朗 ASN 前缀集合”的第一版缓存链路；下一步最自然的是在这个
  prefix inventory 之上接 prefix_geo。